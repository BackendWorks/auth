export enum APP_ENVIRONMENT {
    PRODUCTION = 'production',
    STAGING = 'staging',
    DEVELOPMENT = 'development',
    TEST = 'testing',
    LOCAL = 'local',
}

export enum SORT_ORDER {
    ASC = 'asc',
    DESC = 'desc',
}
